// issue 032：本文件由 Rust 生成，勿手改。重新生成：cargo run -p agent-server --features ts --example gen_protocol_ts

// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { Adjustment } from "./Adjustment";
import type { DriftVerdict } from "./DriftVerdict";
import type { GuardReport } from "./GuardReport";
import type { Notice } from "./Notice";
import type { TokenUsage } from "./TokenUsage";
import type { ToolCallId } from "./ToolCallId";
import type { ToolCallRequest } from "./ToolCallRequest";
import type { UndoOutcome } from "./UndoOutcome";

/**
 * 一个 session 广播的一件事。`Clone + Send + 'static`（`broadcast` 的硬要求）
 * 且全部可序列化（032 的前提）——本文件模块文档记了为什么不是直接用
 * `RunnerEvent`，以及 `tag`/`content` 这两个 serde 属性为什么这么选。
 *
 * 032：TS 类型从这里生成，`ts` feature 门后面（`crate::ts_protocol`）。**不改
 * 任何 serde 属性**——生成器适配协议，不是协议适配生成器（issue 032 注意事项）。
 */
export type SessionEvent = { "type": "text_delta", "data": string } | { "type": "thinking_delta", "data": string } | { "type": "tool_call_started", "data": { name: string, } } | { "type": "preflight_drift_alert", "data": DriftVerdict } | { "type": "transport_trouble", "data": string } | { "type": "tool_executing", "data": { call_id: ToolCallId, request: ToolCallRequest, } } | { "type": "tool_executed", "data": { call_id: ToolCallId, tool: string, output_len: number, is_error: boolean, } } | { "type": "turn_guard", "data": { usage: TokenUsage, report: GuardReport, adjustments: Array<Adjustment>, } } | { "type": "notice", "data": Notice } | { "type": "undo", "data": UndoOutcome } | { "type": "redo", "data": UndoOutcome } | { "type": "lagged", "data": { skipped: number, } } | { "type": "session_died", "data": { reason: string, } } | { "type": "gap", "data": { skipped: number, } };
