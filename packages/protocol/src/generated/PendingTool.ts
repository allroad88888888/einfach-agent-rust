// issue 032：本文件由 Rust 生成，勿手改。重新生成：cargo run -p agent-server --features ts --example gen_protocol_ts

// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AgentId } from "./AgentId";
import type { ToolCallId } from "./ToolCallId";
import type { ToolCallRequest } from "./ToolCallRequest";

/**
 * 一条还欠着宿主回传的远端调用。
 *
 * 字段名跟 `SessionEvent::ToolExecuting` 的载荷对齐（`call_id` + `request`）：
 * 宿主的两条路——收到帧、或者连上就拉一次——拿到的是同一种东西，执行那一段代码
 * 因此只有一份。
 *
 * **不带 `epoch`**：epoch 是服务端保管的凭据，客户端伪造不了也不该看见
 * （`crate::http::routes::tool_result` 模块文档写死了这条）。**不带截止线**：
 * 那是 060 的内部账，宿主拿它做不了任何正确的决定——它该做的只有「还欠着就执行」。
 */
export type PendingTool = { agent: AgentId, call_id: ToolCallId, request: ToolCallRequest, };
