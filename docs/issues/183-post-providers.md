# 183 文章：三家 provider 的真实差异（实测）

**里程碑** L · **依赖** [165](165-launch-positioning-decision.md) · **模型** **opus** · **估时** 20min（初稿） · **状态** 初稿完成（2026-08-13）

## 目标

把 `probes/PROVIDERS.md`（301 行实测）改写成一篇能发的文章。

**赌它是单篇流量最高的一件**：全网几乎没有人公开发过 DeepSeek / Kimi / GLM 的
逐条对比实测，尤其带 prompt cache 命中率的真实数字。这类内容的特点是
**长尾搜索流量**——发出去之后一年里持续有人搜到。

## 素材

- `probes/PROVIDERS.md` —— 唯一结论文档
- `probes/results/` —— 原始观测
- 决策 17 / 决策 18 的取舍（差异是怎么被消化掉的）

## 写什么

标题方向：*What actually differs between DeepSeek, Kimi and GLM — measured, not documented*

1. 为什么要实测：三家都自称 OpenAI 兼容，**文档说的和实际返回的不一样**
2. 逐条差异（拿真实响应片段，不是转述）
3. **prompt cache 那一节是重点**——命中率的真实数字、什么动作会打掉缓存、代价多大
   （「DeepSeek 上 120 倍」这个数字很有冲击力，但要写清它的前提）
4. 结尾轻轻带一句这些差异在 einfach-agent 里被 adapter 消化掉了，**给个链接就够**
   —— 文章的价值必须独立于项目存在，硬推销会毁掉转发

## 验收

- **不读代码、不用这个项目的人也能获得价值**（这条是硬标准，做不到就重写）
- 每个结论都能追溯到 `probes/results/` 里的一次真实观测
- 数字与 `PROVIDERS.md` 一致，**没有为了效果四舍五入**
- 项目链接**不超过两处**，且都在自然的位置

## 渠道

英文优先（[165](165-launch-positioning-decision.md) L1）。中文版价值同样高，
排 L2 第二波。

---

## 实做记录（2026-08-13）

**初稿在 [`docs/posts/183-three-providers.en.md`](../posts/183-three-providers.en.md)。**

### 开篇没用「我做了实测」，用的是「文档两个方向都错」

原计划的开篇是「为什么要实测：三家都自称 OpenAI 兼容，文档说的和实际返回的不一样」。
写的时候发现素材里有更硬的东西——**不是「文档不全」，是文档在两个相反方向上都错**：

- DeepSeek 文档说 `tool_choice` 四种全支持，实际两种直接 400
- GLM 文档说只支持 `auto`，实际四种全支持

**「两个方向」才是关键**：如果错误方向一致，读者还能施加一个统一的修正；两个方向都错
就意味着文档整体不可作为依据。而且两种错法的后果不同——按 DeepSeek 文档写会崩，
按 GLM 文档写会白白降级，**两个都会看起来像你自己的 bug**。这一段现在是全文的地基。

### 三条主线，按「读者会付出什么代价」排

不按「缓存 / 工具 / 流式」这种素材本身的结构排，那是给写的人方便不是给读的人方便。
改成按后果排：

1. **改变你架构的那条**——缓存匹配语义分两类（GLM 真前缀树 vs 另两家仅扩展）。
   它决定「兄弟 agent 共享前缀」这条设计成不成立（只在 GLM 成立），
   也决定压缩一次值多少钱（**DeepSeek 上约等于 120 轮命中**）
2. **会静默搞坏你的那条**——Kimi 的 usage 在 finish 之后另起一帧且 `choices` 为空。
   假定每帧都有 `choices[0]` 的解码器**大概率不是 panic 而是静默丢掉 usage**，
   然后一切看起来正常，而所有缓存兜底从此失明
3. **反直觉的那条**——DeepSeek 上「追加一条 system 消息」把缓存清零（哪怕插入点之前
   逐字节相同），而「改写顶层 system」反而保住缓存。跟另两家完全相反

第 2 条写成了全文的价值主张：**「响得大声的失败是便宜的，一边正常工作一边悄悄
关掉你的仪表盘的才是贵的」**——这也正是本仓红线摘要那套判据，对外讲比对内更有说服力。

### 两个方法论细节留在了文章里

它们比结论更可迁移，所以没删：

- **「归零」不是「作废」**：冷轮 0% 曾被我误读成「改工具表把缓存打掉了」，
  复跑第二轮全部命中才发现旧前缀一直在，0 只是「这个变体第一次见」。
  **两种情况给出同一个数字，含义完全不同。**
- **测「听不听」必须给够 `max_tokens`**：DeepSeek 那次差点被判「未听」，
  实际是默认开着思考、314 token 全进了 reasoning、正文被截成空串。
  **不然你测的是截断，却当成了行为。**

### 数字核对

抽查了 11 个关键数字（5440 / 96.7% / 5276→5382 / 5120 / 3968 / 4352 / 194 / 191 /
640 / 2304），**每一个都在 `probes/PROVIDERS.md` 里有出处**，原始观测在
`probes/results/*.json`（五个文件都在）。文章结尾显式邀请读者复跑并报告差异
——数字会变，方法才是留下来的东西。

### 待办

- [x] ~~中文版（L2 第二波）~~ —— **提前做了**（2026-08-13），在 [`docs/posts/183-three-providers.zh-CN.md`](../posts/183-three-providers.zh-CN.md)。
      L2 是我自己排的顺序，不是被什么挡着；「全部执行完」之下没有理由压着。
      **不是逐句直译**：英文版是先写的，中文版按中文的节奏重写，
      但**所有承重数字逐个核对过两版一致**（脚本比对，不靠眼看）。
- [x] ~~发之前复查 `tool_choice` 那两条~~ —— **重跑了（2026-08-13，距首测 13 天）：
      七格全表 + 两条错误原文逐字复现，地基没塌。**

      跑法留了下来：`PROBE_TOOL_CHOICE_ONLY=1 cargo run --bin wire_shape`
      （三家各三个请求，比全套十几个便宜得多）。结果落在
      **`probes/results/wire-shape-toolchoice-recheck.json`，不覆盖 07-31 那份**
      ——这是这次做的一个有意的选择：覆盖掉就只剩「现在的样子」，
      而要回答的问题是「变没变」，那需要两份带日期的观测并排放着。

      复现的具体内容：DeepSeek 默认思考下 `required` 与指定函数都 400、显式关思考后
      两者都 200；Kimi `required` 通、指定函数 400；GLM 六格全 200。
      错误原文 `Thinking mode does not support this tool_choice` /
      `tool_choice 'specified' is incompatible with thinking enabled` 一字未变——
      **文章里是逐字引用，所以必须核到字符级，核到 HTTP 码相同是不够的。**

      结论写进了 [PROVIDERS.md](../../probes/PROVIDERS.md) §二那张表下面，
      带日期。**13 天零变化不代表以后不变**，那句提醒也一并写了。
- [x] ~~确认 `probes/results/` 真的在版本控制里~~ —— **验过了**：五个 json 全部
      `git ls-files` 有、`git check-ignore` 无命中；探针代码 `probes/api/src/{bin,exp}/`
      也在。文章那句「Raw JSON is in the repo」站得住
